GOOGLE_DRIVE_GRANTS_ROOT_FOLDER_ID=
GEMINI_API_KEY=
SECRET_KEY=
APP_PASSWORD=
# Only needed for headless deploys (Railway, etc.) where there's no browser to
# run the OAuth consent flow. Run `python tools/auth.py` locally, then paste
# the full contents of the generated token.json here as one line.
GOOGLE_TOKEN_JSON=
